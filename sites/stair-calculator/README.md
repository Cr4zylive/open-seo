# Stair calculator

English (US) straight-run rise/run tool. Own Cloudflare Worker (Static Assets).

```bash
cd sites/stair-calculator
node --test
python3 -m http.server 4179
npx wrangler deploy --dry-run
```
